{
  "name": "soins-plus",
  "version": "0.1.0",
  "private": true,
  "description": "Plateforme SOINS+ - module de formation continue pour prestataires de soins en Belgique",
  "packageManager": "pnpm@10.22.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "prettier": "^3.3.3",
    "turbo": "^2.1.3",
    "typescript": "^5.6.3"
  },
  "pnpm": {
    "patchedDependencies": {}
  }
}

